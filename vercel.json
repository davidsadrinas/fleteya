{
  "framework": "nextjs",
  "installCommand": "corepack enable && corepack prepare pnpm@10.28.1 --activate && pnpm install",
  "buildCommand": "pnpm build:web",
  "outputDirectory": "apps/web/.next"
}
